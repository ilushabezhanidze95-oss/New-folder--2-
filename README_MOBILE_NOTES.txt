Mobile nav changes applied: slide-in panel (260px) with overlay.
Behavior:
- Menu toggles with `.mobile-menu-btn`.
- Overlay click closes menu.
- Nav links close menu upon tap (mobile) via added JS.
Testing:
- Open `index.html` in browser and use device toolbar (Chrome DevTools) or open on phone.
- Optional: run a local server: `npx http-server .` or use VS Code Live Server extension.
